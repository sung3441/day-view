import { memo } from 'react';
import { IconButton } from '@/shared/component/Molecule';

const Logout = () => {
  const a = '';
  return <IconButton type="sm_trash" />;
};

export default memo(Logout);
